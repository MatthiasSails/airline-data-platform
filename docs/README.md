# docs/ — Operational & Engineering Documentation

Cross-cutting documentation that does not belong to a single project phase.
Phase deliverables live in the numbered folders (`01-requirements/`,
`02-api-docs/`); module-specific "how to run this" docs live in each code
module's own README (e.g. `03-data-collection/README.md`).

| Document | Purpose |
|---|---|
| [setup.md](setup.md) | Local development setup (venv, kernel, environment) |
| [mongodb-access.md](mongodb-access.md) | MongoDB Atlas team access — users, roles, connection, secrets |

`images/` holds shared assets referenced from the project README and these docs.
